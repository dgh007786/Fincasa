module.exports = {
  // Use the basePath and assetPrefix settings if your site is not at the root of the domain.
  basePath: '/Fincasa/fincasa-webdev', // Replace 'repository-name' with the name of your repository.
  assetPrefix: '/Fincasa/fincasa-webdev',

  // Disable server-side image optimization since it's not supported by static export
  images: {
    loader: 'imgix',
    path: '',
  },

  // Additional configurations here
}
